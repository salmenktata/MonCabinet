# 🏗️ Architecture de Déploiement MonCabinet

Vue d'ensemble de l'architecture de déploiement de MonCabinet sur VPS Contabo avec Cloudflare.

---

## 🌐 Architecture Globale

```
┌─────────────────────────────────────────────────────────────────┐
│                         UTILISATEUR                             │
│                    (Navigateur / Mobile)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTPS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        CLOUDFLARE                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  • CDN Global (Cache statique)                           │  │
│  │  • Protection DDoS                                        │  │
│  │  • WAF (Web Application Firewall)                        │  │
│  │  • SSL/TLS Termination                                   │  │
│  │  • DNS Management                                         │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTPS (Full Strict)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      VPS CONTABO                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     NGINX                                 │  │
│  │  • Reverse Proxy                                          │  │
│  │  • SSL Certificate (Let's Encrypt)                       │  │
│  │  • Load Balancing (si nécessaire)                        │  │
│  │  • Compression Gzip/Brotli                               │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                         │
│                       │ HTTP (localhost)                        │
│                       ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  DOCKER COMPOSE                          │  │
│  │  • Next.js (port 3000)                                   │  │
│  │  • PostgreSQL (port 5432)                                │  │
│  │  • Redis (port 6379)                                     │  │
│  │  • MinIO (ports 9000/9001)                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   FIREWALL (UFW)                         │  │
│  │  • Port 22 (SSH)                                         │  │
│  │  • Port 80 (HTTP)                                        │  │
│  │  • Port 443 (HTTPS)                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │ API Calls
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SERVICES EXTERNES                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ RESEND/BREVO │  │ GOOGLE DRIVE │  │ WHATSAPP/IA  │          │
│  │              │  │              │  │              │          │
│  │ • Email API  │  │ • Storage    │  │ • Webhooks   │          │
│  │ • Templates  │  │ • OAuth      │  │ • LLM APIs   │          │
│  │              │  │              │  │              │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flux de Requêtes

### 1. Requête Utilisateur

```
1. Utilisateur saisit : https://moncabinet.tn
2. DNS lookup → Cloudflare nameservers
3. Cloudflare résout vers l'IP du VPS
```

### 2. Traitement par Cloudflare

```
┌─────────────────────────────────────────┐
│         Cloudflare Edge                 │
├─────────────────────────────────────────┤
│                                         │
│  1. Protection DDoS                     │
│     └─> Vérification requête            │
│                                         │
│  2. WAF (Firewall)                      │
│     └─> Règles de sécurité              │
│                                         │
│  3. Cache Check                         │
│     ├─> Hit ? → Retourne depuis cache   │
│     └─> Miss ? → Passe au origin        │
│                                         │
│  4. SSL/TLS Termination                 │
│     └─> Déchiffrement                   │
│                                         │
│  5. Proxy vers VPS                      │
│     └─> HTTPS (Full Strict Mode)        │
│                                         │
└─────────────────────────────────────────┘
```

### 3. Traitement sur VPS

```
┌─────────────────────────────────────────┐
│            VPS Contabo                  │
├─────────────────────────────────────────┤
│                                         │
│  1. Nginx reçoit la requête (port 443)  │
│     └─> Vérifie SSL Certificate         │
│                                         │
│  2. Reverse Proxy                       │
│     └─> Transfert vers localhost:3000   │
│                                         │
│  3. Docker Compose route vers Next.js   │
│     ├─> Containers internes             │
│     ├─> PostgreSQL / Redis / MinIO      │
│     └─> Restart policies                │
│                                         │
│  4. Next.js traite la requête           │
│     ├─> Server Component rendering      │
│     ├─> API route execution             │
│     ├─> Database queries (PostgreSQL)   │
│     └─> Cache/Storage (Redis/MinIO)     │
│                                         │
│  5. Réponse renvoyée                    │
│     └─> Nginx → Cloudflare → User       │
│                                         │
└─────────────────────────────────────────┘
```

---

## 📁 Structure des Fichiers sur le VPS

```
/opt/moncabinet/
└── moncabinet/                    # Application principale
    ├── .env.production            # Variables d'environnement (secret)
    ├── docker-compose.yml         # Stack Docker (build local)
    ├── docker-compose.prod.yml    # Stack Docker (image pré-buildée)
    ├── app/                       # Source Next.js App Router
    ├── components/                # Composants React
    ├── lib/                       # Bibliothèques et utilitaires
    ├── public/                    # Assets statiques
    ├── db/                        # Migrations SQL
    ├── logs/                      # Logs applicatifs (volume)
    ├── deploy.sh                  # Script de déploiement
    └── backup.sh                  # Script de backup

/etc/nginx/
├── nginx.conf                     # Config principale Nginx
├── sites-available/
│   └── moncabinet.tn              # Config du site
└── sites-enabled/
    └── moncabinet.tn              # Symlink vers sites-available

/var/log/nginx/
├── moncabinet.tn.access.log       # Logs d'accès
└── moncabinet.tn.error.log        # Logs d'erreur

/opt/backups/moncabinet/           # Backups PostgreSQL/MinIO
├── db_20260205_020000.sql.gz
├── minio_20260205/
└── ...

/etc/letsencrypt/
└── live/
    └── moncabinet.tn/             # Certificats SSL
        ├── fullchain.pem
        ├── privkey.pem
        └── chain.pem
```

---

## 🔐 Couches de Sécurité

```
┌──────────────────────────────────────────────────────────────┐
│  COUCHE 1 : Cloudflare                                       │
│  • Protection DDoS                                           │
│  • WAF (Web Application Firewall)                            │
│  • Rate Limiting                                             │
│  • Bot Protection                                            │
└──────────────────────────────────────────────────────────────┘
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  COUCHE 2 : Firewall VPS (UFW)                              │
│  • Ports limités (22, 80, 443)                               │
│  • Règles IP si nécessaire                                   │
└──────────────────────────────────────────────────────────────┘
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  COUCHE 3 : Nginx                                            │
│  • Reverse Proxy                                             │
│  • SSL/TLS Termination                                       │
│  • Rate Limiting (optionnel)                                 │
│  • Header Security (HSTS, CSP)                               │
└──────────────────────────────────────────────────────────────┘
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  COUCHE 4 : Application Next.js                              │
│  • Authentification JWT HttpOnly                             │
│  • Validation des entrées (Zod)                              │
│  • CSRF Protection                                           │
│  • Requêtes SQL paramétrées (pg)                             │
└──────────────────────────────────────────────────────────────┘
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  COUCHE 5 : Services Externes                                │
│  • API Keys protégées (env variables)                        │
│  • OAuth 2.0 (Google Drive)                                  │
│  • HMAC Webhooks (WhatsApp)                                  │
└──────────────────────────────────────────────────────────────┘
```

---

## ⚡ Optimisations Performance

### Cloudflare

```
┌────────────────────────────────────┐
│  Cache Statique                    │
│  • Images                          │
│  • CSS / JS                        │
│  • Fonts                           │
│  • /_next/static/*                 │
│                                    │
│  → TTL: 1 mois                     │
│  → Compression Brotli activée      │
│  → HTTP/2 & HTTP/3 enabled         │
└────────────────────────────────────┘
```

### Nginx

```
┌────────────────────────────────────┐
│  Compression Gzip                  │
│  • HTML, CSS, JS, JSON             │
│                                    │
│  Proxy Cache                       │
│  • Assets statiques                │
│                                    │
│  Connection Keep-Alive             │
│  • Timeout: 60s                    │
└────────────────────────────────────┘
```

### Next.js

```
┌────────────────────────────────────┐
│  Static Generation (SSG)           │
│  • Pages publiques                 │
│                                    │
│  Incremental Static Regeneration   │
│  • Mise à jour périodique          │
│                                    │
│  Server Components                 │
│  • Réduction bundle client         │
│                                    │
│  Image Optimization                │
│  • Format WebP automatique         │
│  • Lazy loading                    │
└────────────────────────────────────┘
```

---

## 📊 Monitoring et Logs

```
┌─────────────────────────────────────────────────────────────┐
│                    MONITORING STACK                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Docker     │  │    Nginx     │  │  Cloudflare  │     │
│  │              │  │              │  │              │     │
│  │ • CPU Usage  │  │ • Access Log │  │ • Analytics  │     │
│  │ • Memory     │  │ • Error Log  │  │ • Threats    │     │
│  │ • Restarts   │  │ • Status     │  │ • Cache Hit  │     │
│  │ • Logs       │  │              │  │   Ratio      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                             │
│  Commandes :                                                │
│  • docker compose ps                                        │
│  • docker compose logs -f --tail=100                        │
│  • tail -f /var/log/nginx/moncabinet.tn.error.log           │
│  • Cloudflare Dashboard Analytics                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Processus de Déploiement

```
┌─────────────────────────────────────────────────────────────┐
│                  WORKFLOW DE DEPLOIEMENT                    │
└─────────────────────────────────────────────────────────────┘

Developer Machine
       │
       │ git push
       ▼
   GitHub Repository
       │
       │ git pull (manuel ou CI/CD)
       ▼
   VPS Contabo
       │
       ├─> docker compose build   (Build images)
       │
       ├─> docker compose up -d   (Restart containers)
       │
       └─> health check           (API /health)

Automatisé par : ./deploy.sh
```

---

## 💾 Stratégie de Backup

```
┌─────────────────────────────────────────────────────────────┐
│                    BACKUP STRATEGY                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Quotidien (2h du matin)                                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  • Application Code                                   │  │
│  │  • Configuration (.env.production)                   │  │
│  │  • Stocké dans /opt/backups/moncabinet/               │  │
│  │  • Rotation : 14 jours                                │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  Base de Données (PostgreSQL)                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  • Dump SQL via docker exec                           │  │
│  │  • Backups quotidiens                                 │  │
│  │  • Compression gzip                                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  Stockage Documents (MinIO)                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  • Mirror bucket documents                            │  │
│  │  • Backups quotidiens                                 │  │
│  │  • Stockés avec les backups                           │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  Certificats SSL                                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  • Renouvellement automatique (Certbot)               │  │
│  │  • Stockés dans /etc/letsencrypt/                     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Points Clés de l'Architecture

### Scalabilité

- **Horizontal** : Possibilité d'ajouter des VPS derrière Cloudflare
- **Vertical** : Upgrade facile du VPS (CPU/RAM)
- **Database** : PostgreSQL auto-hébergé (scale vertical ou service managé)

### Haute Disponibilité

- **Cloudflare CDN** : 300+ datacenters mondiaux
- **Docker** : Restart policies + health checks
- **Let's Encrypt** : Renouvellement SSL automatique

### Performance

- **CDN Global** : Cloudflare cache les assets statiques
- **Compression** : Brotli/Gzip sur tous les assets
- **HTTP/3** : Protocole moderne pour vitesse optimale

### Sécurité

- **5 couches** de protection (voir section Sécurité)
- **SSL/TLS** : Certificat gratuit avec renouvellement automatique
- **Firewall** : UFW + Cloudflare WAF

---

## 📈 Métriques à Surveiller

### Performance

- Temps de réponse moyen (target: < 200ms)
- Cache hit ratio Cloudflare (target: > 80%)
- Lighthouse Score (target: > 90)

### Disponibilité

- Uptime (target: 99.9%)
- Erreurs 5xx (target: < 0.1%)
- Temps de résolution DNS (target: < 50ms)

### Ressources VPS

- CPU usage (target: < 70%)
- Memory usage (target: < 80%)
- Disk space (target: > 20% libre)

---

**🏗️ Architecture conçue pour la performance, la sécurité et la scalabilité !**
