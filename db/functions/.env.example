# Supabase Edge Functions - Variables d'environnement

# Supabase (automatiquement injectées)
SUPABASE_URL=https://vgaofkucdpydyblrykbh.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Resend API (Email notifications)
# Obtenir une clé sur: https://resend.com
# Plan gratuit: 3000 emails/mois
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# URL de base de l'application
# Production: https://avocat.tn
# Dev: http://localhost:3000
BASE_URL=https://avocat.tn

# Secret pour authentifier les Cron jobs
# Générer avec: openssl rand -base64 32
CRON_SECRET=your-secure-random-string-here

# Configuration
# IMPORTANT: Ces secrets doivent être configurés via:
# supabase secrets set RESEND_API_KEY=re_xxx
# supabase secrets set BASE_URL=https://avocat.tn
# supabase secrets set CRON_SECRET=$(openssl rand -base64 32)

# Ou via Dashboard Supabase > Edge Functions > Settings
